# Choice string injection

We expect the ${choice} strings to be replaced with AAA/BBB, respectively.

=== "AAA"
    ```shell
    echo ${choice}
    ```

=== "BBB"
    ```shell
    echo ${choice}
    ```
