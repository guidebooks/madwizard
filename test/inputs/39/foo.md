---
validate: true
---

```shell
echo "Bug if any of this is printed."
```

=== "Tab1"
    ```shell
    echo "Bug if this is printed"
    ```

=== "Tab2"
    ```shell
    echo "Also a bug if this is printed"
    ```
